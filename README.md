# FastAPI Template

Venv

```bash
python3 -m venv venv

.\venv\Scripts\activate
```

Install

```bash
pip install -r requirements.txt
```

Running

```bash
python main.py
```

Docker (MySQL, Adminer, Redis)

```bash
docker-compose up -d
```
 
